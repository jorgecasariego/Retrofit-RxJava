<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jorgecasariego.retrofit">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.ExampleListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.GithubActivity" />
        <activity android:name=".activity.Example1Activity" />
        <activity android:name=".activity.Example2Activity" />
        <activity android:name=".activity.Example3Activity" />
        <activity android:name=".activity.Example4Activity" />
        <activity android:name=".activity.Example5Activity" />
        <activity android:name=".activity.Example6Activity"></activity>
    </application>

</manifest>